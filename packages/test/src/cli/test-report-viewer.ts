import { mkdirSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';

const viewerCss = String.raw`
:root{color-scheme:light;font-family:"IBM Plex Sans","Segoe UI Variable","Segoe UI",sans-serif;color:#172033;background:#eef1f5;--ink:#172033;--muted:#69758a;--line:#d9dfe8;--panel:#fff;--nav:#111a2b;--nav2:#19263d;--accent:#f0b429;--blue:#2f63c7;--green:#16824b;--red:#c73549;--blocked:#967530}*{box-sizing:border-box}body{margin:0;min-width:980px;background:#eef1f5}button,input,select{font:inherit}button,summary,select{outline-color:var(--accent)}.topbar{min-height:72px;padding:15px 26px;display:flex;align-items:center;justify-content:space-between;color:#f8fafc;background:var(--nav);border-bottom:3px solid var(--accent)}.brand{display:flex;align-items:center;gap:12px;font-weight:760;letter-spacing:-.02em}.brand-mark{width:9px;height:26px;border-radius:2px;background:var(--accent);box-shadow:14px 0 0 #385b93}.run{margin-top:4px;color:#9fafc8;font:11px/1.4 ui-monospace,monospace}.status,.status-chip{display:inline-flex;align-items:center;border-radius:999px;font-size:11px;font-weight:760;text-transform:uppercase;letter-spacing:.05em}.status{padding:7px 11px;color:#baf0d1;background:#173b2b}.status.failed{color:#ffd3d8;background:#4b2029}.layout{display:grid;grid-template-columns:360px minmax(0,1fr);min-height:calc(100vh - 72px)}.sidebar{overflow:auto;padding:18px 16px 34px;color:#dce5f3;background:var(--nav);border-right:1px solid #28364d}.summary{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin-bottom:14px}.metric{padding:9px 7px;border:1px solid #2b3850;border-radius:7px;background:#172238}.metric b{display:block;margin-bottom:2px;color:#fff;font-size:17px}.metric span{color:#8fa0ba;font-size:9px;text-transform:uppercase;letter-spacing:.06em}.filter{width:100%;margin-bottom:14px;padding:9px 11px;color:#edf3fc;border:1px solid #34445e;border-radius:7px;background:#172238}.filter::placeholder{color:#8291a9}.project{margin:8px 0;border:1px solid #293850;border-radius:8px;overflow:hidden;background:#141f33}.project>summary,.document>summary{list-style:none;cursor:pointer}.project>summary::-webkit-details-marker,.document>summary::-webkit-details-marker{display:none}.project>summary{display:flex;align-items:center;gap:8px;padding:10px;font-size:12px;font-weight:750;background:var(--nav2)}.project>summary:before,.document>summary:before{content:"›";color:#7f91ae;transition:transform .15s}.project[open]>summary:before,.document[open]>summary:before{transform:rotate(90deg)}.project-meta{margin-left:auto;color:#8495af;font-size:10px;font-weight:500}.document{border-top:1px solid #27354b}.document>summary{display:flex;align-items:center;gap:4px;padding:9px 10px 7px;color:#aab8ce;font:10px/1.3 ui-monospace,monospace;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.nav-item{width:calc(100% - 10px);margin:2px 5px;display:grid;grid-template-columns:14px minmax(0,1fr);gap:8px;padding:8px 9px;color:#dce5f3;border:0;border-radius:6px;text-align:left;cursor:pointer;background:transparent}.nav-item:hover{background:#22314a}.nav-item.selected{color:#fff;background:#29405f;box-shadow:inset 3px 0 var(--accent)}.nav-item.issue{color:#ffd1d7}.nav-title{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px;font-weight:650}.nav-meta{display:block;margin-top:3px;color:#8394ae;font-size:10px}.dot{width:8px;height:8px;margin-top:4px;border-radius:50%;background:#31ad6a;box-shadow:0 0 0 3px rgb(49 173 106/12%)}.dot.failed{background:#f05b6e;box-shadow:0 0 0 3px rgb(240 91 110/13%)}.dot.blocked{background:#d3a640;box-shadow:0 0 0 3px rgb(211 166 64/13%)}.dot.info{background:#7095d0;box-shadow:0 0 0 3px rgb(112 149 208/13%)}.main{min-width:0;overflow:auto;padding:28px 34px 46px}.empty,.loading{padding:64px 24px;text-align:center;color:var(--muted)}.detail{max-width:1480px;margin:0 auto}.case-head{padding:22px 24px;border:1px solid var(--line);border-radius:11px;background:var(--panel);box-shadow:0 7px 24px rgb(24 35 54/6%)}.eyebrow{color:var(--muted);font:11px/1.4 ui-monospace,monospace}.title-row{display:flex;gap:12px;align-items:center;margin-top:7px}.title-row h1{flex:1;min-width:0;margin:0;font-size:25px;letter-spacing:-.035em}.status-chip{padding:5px 8px;color:#17663d;background:#e1f5e9}.status-chip.failed{color:#9c2435;background:#fde7ea}.status-chip.blocked{color:#745a20;background:#f7edd5}.details{margin-top:7px;color:var(--muted);font-size:12px}.failure{margin-top:16px;padding:12px 14px;border-left:4px solid var(--red);border-radius:6px;color:#7f2634;background:#fff0f2;font-size:13px;line-height:1.5}.toolbar{display:flex;flex-wrap:wrap;gap:9px;align-items:center;margin-top:17px}.toolbar label{color:var(--muted);font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.04em}.toolbar select{min-width:175px;padding:7px 9px;border:1px solid #cbd4e0;border-radius:6px;color:var(--ink);background:#fff}.tabs{display:flex;gap:3px;margin:18px 0 0;padding-left:7px;border-bottom:1px solid #cfd7e2}.tab{padding:10px 15px;border:0;border-bottom:3px solid transparent;color:#657187;background:transparent;cursor:pointer;font-weight:700}.tab.active{color:#1e4f9e;border-color:var(--blue)}.content-panel{min-height:360px;padding-top:18px}.replay-shell{overflow:hidden;border:1px solid #cdd6e2;border-radius:10px;background:#0c1320;box-shadow:0 8px 25px rgb(24 35 54/8%)}.replay-toolbar{min-height:48px;padding:8px 12px;display:flex;gap:10px;align-items:center;color:#d8e2ef;background:#151f30;border-bottom:1px solid #2b384d}.replay-toolbar select{min-width:180px;padding:6px 8px;color:#e8eef7;border:1px solid #40506a;border-radius:5px;background:#202d43}.report-link{margin-left:auto;padding:7px 10px;border:1px solid #63748f;border-radius:5px;color:#f3f6fb;text-decoration:none;font-size:12px}.report-link:hover{border-color:var(--accent);color:var(--accent)}.frame-wrap{height:min(68vh,760px);min-height:480px;display:grid;place-items:center;color:#9babbe}.frame-wrap iframe{width:100%;height:100%;border:0;background:#fff}.section{margin-top:16px}.section h2{margin:0 0 9px;color:#4f5c70;font-size:12px;text-transform:uppercase;letter-spacing:.05em}.step,.diagnostic{margin:7px 0;border:1px solid var(--line);border-radius:8px;background:#fff}.step.failed{border-color:#e997a3}.step summary{display:flex;gap:10px;align-items:center;padding:12px 14px;cursor:pointer;list-style:none}.step summary::-webkit-details-marker{display:none}.step summary:before{content:"+";width:18px;color:#748096;font:14px ui-monospace,monospace}.step[open] summary:before{content:"−"}.step-status{font-size:10px;font-weight:800;color:var(--green)}.step-status.failed{color:var(--red)}.step-name{font-weight:720}.step-time{margin-left:auto;color:#718099;font-size:11px}.step-summary{padding:0 14px 11px 42px;color:#4f5b70;font-size:12px;line-height:1.5}.step-detail{padding:0 14px 14px 42px}pre{overflow:auto;margin:7px 0 12px;padding:11px;border-radius:6px;color:#dce9ff;background:#172238;font:11px/1.5 ui-monospace,monospace}.copy{padding:6px 9px;border:1px solid #cbd5e3;border-radius:5px;color:#254b89;background:#fff;cursor:pointer;font-size:11px}.diagnostic{padding:16px}.diagnostic h2{margin:0 0 10px;font-size:14px}.error{padding:16px;color:#8e2636;border:1px solid #edb5bd;border-radius:8px;background:#fff0f2}@media(prefers-reduced-motion:reduce){*{transition:none!important}}
.no-replay{align-self:center;margin-left:auto;padding:5px 9px;color:#7a8598;font-size:11px}
`;

const viewerScript = String.raw`
const state={summary:null,selections:[],selected:null,attemptId:null,reportIndex:0,view:null,renderToken:0,expandedProjects:new Set(),expandedDocuments:new Set()};
const text=value=>value==null?'':String(value);
const duration=ms=>ms<1000?ms+' ms':(ms/1000).toFixed(ms>=10000?0:1)+' s';
const element=(tag,className,value)=>{const node=document.createElement(tag);if(className)node.className=className;if(value!==undefined)node.textContent=value;return node};
const statusClass=status=>status==='failed'?'failed':status==='not-run'?'blocked':'';
const reportHref=(report,embedded)=>{const url=new URL('../'+report.path,location.href);if(embedded){url.searchParams.set('player-only','1');url.searchParams.set('play-control','1');url.searchParams.set('auto-play','0')}return url.href};
async function json(path){const response=await fetch('../'+path);if(!response.ok)throw new Error('Could not load '+path+' ('+response.status+')');return response.json()}
async function ensureAttempt(attempt){if(!attempt||attempt.data)return attempt&&attempt.data;attempt.data=await json(attempt.resultFile);return attempt.data}
async function ensureDocument(item){if(!item||item.data)return item&&item.data;item.data=await json(item.resultFile);return item.data}
const failureOf=data=>data&&[...(data.beforeEach||[]),...(data.steps||[]),...(data.afterEach||[]),...(data.beforeAll||[]),...(data.afterAll||[])].find(step=>step.status==='failed');
const selectionKey=selection=>selection?[selection.kind,selection.project&&selection.project.projectId,selection.document&&selection.document.documentId,selection.item&&selection.item.caseId,selection.errorIndex].join(':'):'';
const reportsFor=(selection,attempt)=>selection.kind==='case'?((attempt&&attempt.reports)||[]):selection.kind==='document'?(selection.document.reports||[]):[];
const hashParams=()=>new URLSearchParams(location.hash.slice(1));
function writeHash(){const selection=state.selected;if(!selection)return;const params=new URLSearchParams();params.set('kind',selection.kind);params.set('project',selection.project.projectId);if(selection.document)params.set('document',selection.document.documentId);if(selection.item)params.set('case',selection.item.caseId);if(selection.errorIndex!==undefined)params.set('error',String(selection.errorIndex));if(state.attemptId)params.set('attempt',state.attemptId);if(state.view)params.set('view',state.view);if(state.reportIndex)params.set('report',String(state.reportIndex));history.replaceState(null,'','#'+params)}
function currentAttempt(selection){const attempts=selection.kind==='case'?(selection.item.attempts||[]):[];return attempts.find(attempt=>attempt.attemptId===state.attemptId)||attempts[attempts.length-1]}
function renderSummary(){const summary=state.summary.summary;const values=[[summary.total,'Cases'],[summary.failed,'Failed'],[summary.passed,'Passed'],[summary.notRun,'Blocked']];document.getElementById('summary').replaceChildren(...values.map(([value,label])=>{const box=element('div','metric');box.append(element('b','',value),element('span','',label));return box}))}
function navButton(selection,title,meta,status){const button=element('button','nav-item'+(selectionKey(state.selected)===selectionKey(selection)?' selected':'')+(selection.kind.includes('error')?' issue':''));button.type='button';const dot=element('span','dot '+(selection.kind.includes('error')?'failed':statusClass(status)||'info'));const copy=element('span');copy.append(element('span','nav-title',title),element('span','nav-meta',meta));button.append(dot,copy);button.onclick=()=>select(selection,true);return button}
const projectHasFailure=project=>project.status==='failed'||project.cases.some(item=>item.status!=='success')||project.documents.some(item=>item.status==='failed')||project.collectionErrors.length>0;
function buildSelections(){const selections=[];for(const project of state.summary.projects){if(project.lifecycle&&(project.lifecycle.setupError||(project.lifecycle.teardownErrors||[]).length))selections.push({kind:'project-error',project,rank:2});project.collectionErrors.forEach((error,errorIndex)=>selections.push({kind:'collection-error',project,error,errorIndex,rank:1}));for(const documentItem of project.documents){selections.push({kind:'document',project,document:documentItem,rank:documentItem.status==='failed'?1:5});for(const item of project.cases.filter(candidate=>candidate.documentId===documentItem.documentId))selections.push({kind:'case',project,document:documentItem,item,rank:item.status==='failed'?0:item.status==='not-run'?2:4})}}state.selections=selections}
function renderNavigation(){const root=document.getElementById('case-list');const query=document.getElementById('filter').value.trim().toLowerCase();root.replaceChildren();for(const project of state.summary.projects){const projectText=[project.name,project.platform,...project.cases.map(item=>item.name+' '+item.sourcePath),...project.collectionErrors.map(item=>item.sourcePath)].join(' ').toLowerCase();if(query&&!projectText.includes(query))continue;const projectKey=project.projectId;const projectNode=element('details','project');const projectSelected=state.selected&&state.selected.project===project;projectNode.open=state.expandedProjects.has(projectKey)||projectHasFailure(project)||Boolean(query)||projectSelected;projectNode.ontoggle=()=>{if(projectNode.open)state.expandedProjects.add(projectKey);else state.expandedProjects.delete(projectKey)};const projectSummary=element('summary');projectSummary.append(element('span','dot '+statusClass(project.status)),element('span','',project.name),element('span','project-meta',project.platform));projectNode.append(projectSummary);const projectIssue=state.selections.find(selection=>selection.kind==='project-error'&&selection.project===project);if(projectIssue)projectNode.append(navButton(projectIssue,'Project lifecycle','Setup or teardown failure','failed'));state.selections.filter(selection=>selection.kind==='collection-error'&&selection.project===project).forEach(selection=>projectNode.append(navButton(selection,'Collection error',selection.error.sourcePath,'failed')));for(const documentItem of project.documents){const documentKey=project.projectId+':'+documentItem.documentId;const documentNode=element('details','document');const documentFailed=documentItem.status==='failed'||project.cases.some(item=>item.documentId===documentItem.documentId&&item.status!=='success');const documentSelected=state.selected&&state.selected.document===documentItem;documentNode.open=state.expandedDocuments.has(documentKey)||documentFailed||Boolean(query)||documentSelected;documentNode.ontoggle=()=>{if(documentNode.open)state.expandedDocuments.add(documentKey);else state.expandedDocuments.delete(documentKey)};documentNode.append(element('summary','',documentItem.sourcePath));const documentSelection=state.selections.find(selection=>selection.kind==='document'&&selection.project===project&&selection.document===documentItem);documentNode.append(navButton(documentSelection,'Setup & teardown',documentItem.status,documentItem.status));state.selections.filter(selection=>selection.kind==='case'&&selection.project===project&&selection.document===documentItem).forEach(selection=>{const item=selection.item;const count=(item.attempts||[]).length;const meta=item.status==='not-run'?'Blocked · '+text(item.notRunReason):item.status+' · '+count+' attempt'+(count===1?'':'s');documentNode.append(navButton(selection,item.name,meta,item.status))});projectNode.append(documentNode)}root.append(projectNode)}}
function stepCard(step){const failed=step.status==='failed';const card=element('details','step'+(failed?' failed':''));const summary=element('summary');summary.append(element('span','step-status'+(failed?' failed':''),failed?'FAILED':'PASSED'),element('span','step-name',step.node),element('span','step-time',duration(step.durationMs)));card.append(summary);const message=text(step.error&&step.error.message)||text(step.output&&step.output.summary);if(message)card.append(element('div','step-summary',message));const detail=element('div','step-detail');detail.append(element('strong','','Input'));const input=element('pre');input.textContent=JSON.stringify(step.input,null,2);detail.append(input);if(step.error||step.output){detail.append(element('strong','',step.error?'Error':'Output'));const output=element('pre');output.textContent=JSON.stringify(step.error||step.output,null,2);detail.append(output)}const copy=element('button','copy','Copy diagnostic');copy.type='button';copy.onclick=()=>{navigator.clipboard.writeText(JSON.stringify({node:step.node,phase:step.phase,input:step.input,error:step.error,output:step.output},null,2));copy.textContent='Copied';setTimeout(()=>copy.textContent='Copy diagnostic',1200)};detail.append(copy);card.append(detail);return card}
function stepsSection(title,steps){if(!steps||!steps.length)return null;const section=element('section','section');section.append(element('h2','',title));steps.forEach(step=>section.append(stepCard(step)));return section}
function appendSteps(root,documentData,attemptData){const sections=[['beforeAll',documentData&&documentData.beforeAll],['beforeEach',attemptData&&attemptData.beforeEach],['Steps',attemptData&&attemptData.steps],['afterEach',attemptData&&attemptData.afterEach],['afterAll',documentData&&documentData.afterAll]];let count=0;for(const [title,steps] of sections){const section=stepsSection(title,steps);if(section){root.append(section);count+=1}}if(!count)root.append(element('div','empty','No executed steps are available for this selection.'))}
function appendTabs(root,reports){const tabs=element('div','tabs');if(reports.length){const replay=element('button','tab'+(state.view==='replay'?' active':''),'Replay');replay.type='button';replay.onclick=()=>{state.view='replay';state.reportIndex=0;writeHash();void renderDetail()};tabs.append(replay)}const steps=element('button','tab'+(state.view==='steps'?' active':''),'Steps');steps.type='button';steps.onclick=()=>{state.view='steps';writeHash();void renderDetail()};tabs.append(steps);if(!reports.length)tabs.append(element('span','no-replay','No replay artifact'));root.append(tabs)}
async function renderReplay(root,reports,token){if(!reports.length){root.append(element('div','empty','This execution did not produce a replayable Agent report.'));return}const selectedIndex=Math.min(state.reportIndex,reports.length-1);const report=reports[selectedIndex];const shell=element('div','replay-shell');const toolbar=element('div','replay-toolbar');if(reports.length>1){const selectNode=element('select');reports.forEach((item,index)=>{const option=element('option','',item.name||'Report '+(index+1));option.value=String(index);option.selected=index===selectedIndex;selectNode.append(option)});selectNode.onchange=()=>{state.reportIndex=Number(selectNode.value);writeHash();void renderDetail()};toolbar.append(element('span','','Report'),selectNode)}else toolbar.append(element('span','',report.name||'Execution report'));const full=element('a','report-link','Open full report ↗');full.href=reportHref(report,false);full.target='_blank';full.rel='noopener noreferrer';toolbar.append(full);const frameWrap=element('div','frame-wrap');frameWrap.append(element('div','loading','Checking replay artifact…'));shell.append(toolbar,frameWrap);root.append(shell);try{const response=await fetch(reportHref(report,false),{method:'HEAD'});if(!response.ok)throw new Error('Replay artifact returned HTTP '+response.status+': '+report.path);if(token!==state.renderToken)return;const frame=element('iframe');frame.title='Midscene execution replay';frame.src=reportHref(report,true);frame.loading='eager';frameWrap.replaceChildren(frame)}catch(error){if(token!==state.renderToken)return;frameWrap.replaceChildren(element('div','error',text(error&&error.message||error)))}}
function header(selection,attempt,data){const item=selection.kind==='case'?selection.item:selection.document;const title=selection.kind==='case'?item.name:selection.kind==='document'?'Setup & teardown':selection.kind==='project-error'?'Project lifecycle':'Collection error';const status=item&&item.status||'failed';const root=element('div','case-head');root.append(element('div','eyebrow',selection.project.name+(selection.document?' / '+selection.document.sourcePath:'')));const row=element('div','title-row');row.append(element('h1','',title),element('span','status-chip '+statusClass(status),status==='not-run'?'blocked':status));root.append(row);if(attempt)root.append(element('div','details','Attempt '+(attempt.attemptIndex+1)+' · '+duration(data&&data.durationMs||0)));else if(data&&data.durationMs!==undefined)root.append(element('div','details',duration(data.durationMs)));const failed=failureOf(data);if(failed)root.append(element('div','failure','Failed at '+failed.phase+' step '+(failed.stepIndex+1)+': '+(text(failed.error&&failed.error.message)||text(failed.output&&failed.output.summary))));return root}
async function renderCase(selection,root,token){const attempts=selection.item.attempts||[];const attempt=currentAttempt(selection);const [attemptData,documentData]=await Promise.all([ensureAttempt(attempt),ensureDocument(selection.document)]);if(token!==state.renderToken)return;const reports=reportsFor(selection,attempt);if(!reports.length)state.view='steps';if(!state.view)state.view=reports.length?'replay':'steps';const head=header(selection,attempt,attemptData);if(attempts.length>1){const toolbar=element('div','toolbar');toolbar.append(element('label','','Attempt'));const selectNode=element('select');attempts.forEach(item=>{const option=element('option','','Attempt '+(item.attemptIndex+1)+' · '+item.status.toUpperCase());option.value=item.attemptId;option.selected=item===attempt;selectNode.append(option)});selectNode.onchange=()=>{state.attemptId=selectNode.value;state.reportIndex=0;const next=currentAttempt(selection);state.view=((next&&next.reports)||[]).length?'replay':'steps';writeHash();void renderDetail()};toolbar.append(selectNode);head.append(toolbar)}root.append(head);appendTabs(root,reports);const panel=element('div','content-panel');root.append(panel);if(state.view==='replay')await renderReplay(panel,reports,token);else appendSteps(panel,documentData,attemptData)}
async function renderDocument(selection,root,token){const data=await ensureDocument(selection.document);if(token!==state.renderToken)return;const reports=reportsFor(selection);if(!reports.length)state.view='steps';if(!state.view)state.view=reports.length?'replay':'steps';root.append(header(selection,null,data));appendTabs(root,reports);const panel=element('div','content-panel');root.append(panel);if(state.view==='replay')await renderReplay(panel,reports,token);else appendSteps(panel,data,null)}
async function renderDiagnostic(selection,root,token){let title,value;if(selection.kind==='collection-error'){title='Collection error · '+selection.error.sourcePath;value=await json(selection.error.errorFile)}else{title='Project lifecycle · '+selection.project.name;value=selection.project.lifecycle}if(token!==state.renderToken)return;root.append(header(selection,null,null));const panel=element('div','diagnostic');panel.append(element('h2','',title));const pre=element('pre');pre.textContent=JSON.stringify(value,null,2);panel.append(pre);root.append(panel)}
async function renderDetail(){const main=document.getElementById('main');const token=++state.renderToken;main.replaceChildren(element('div','loading','Loading selection…'));if(!state.selected){main.replaceChildren(element('div','empty','This run contains no results.'));return}const root=element('div','detail');try{if(state.selected.kind==='case')await renderCase(state.selected,root,token);else if(state.selected.kind==='document')await renderDocument(state.selected,root,token);else await renderDiagnostic(state.selected,root,token);if(token!==state.renderToken)return;main.replaceChildren(root);writeHash()}catch(error){if(token!==state.renderToken)return;main.replaceChildren(element('div','error',text(error&&error.message||error)))}}
function select(selection,reset){const changed=selectionKey(state.selected)!==selectionKey(selection);state.selected=selection;if(changed||reset){const attempts=selection.kind==='case'?(selection.item.attempts||[]):[];const attempt=attempts[attempts.length-1];state.attemptId=attempt?attempt.attemptId:null;const reports=reportsFor(selection,attempt);state.reportIndex=0;state.view=reports.length?'replay':'steps'}renderNavigation();writeHash();void renderDetail()}
function restoreSelection(){const params=hashParams();const wanted=state.selections.find(selection=>{if(selection.kind!==params.get('kind')||selection.project.projectId!==params.get('project'))return false;if(selection.document&&selection.document.documentId!==params.get('document'))return false;if(selection.item&&selection.item.caseId!==params.get('case'))return false;if(selection.errorIndex!==undefined&&String(selection.errorIndex)!==params.get('error'))return false;return true});const selected=wanted||[...state.selections].sort((a,b)=>a.rank-b.rank)[0];if(!selected)return;state.selected=selected;const attempts=selected.kind==='case'?(selected.item.attempts||[]):[];const requestedAttempt=attempts.find(attempt=>attempt.attemptId===params.get('attempt'));state.attemptId=(requestedAttempt||attempts[attempts.length-1]||{}).attemptId||null;const reports=reportsFor(selected,currentAttempt(selected));const requestedView=params.get('view');state.view=requestedView==='steps'||requestedView==='replay'&&reports.length?requestedView:reports.length?'replay':'steps';const requestedReport=Number(params.get('report'));state.reportIndex=Number.isInteger(requestedReport)&&requestedReport>=0&&requestedReport<reports.length?requestedReport:0}
async function start(){try{const response=await fetch('../summary.json');if(!response.ok)throw new Error('Could not load summary.json ('+response.status+')');state.summary=await response.json();if(state.summary.schemaVersion!==4)throw new Error('Unsupported test report schema: '+state.summary.schemaVersion);document.getElementById('run').textContent=state.summary.runId+' · '+new Date(state.summary.startedAt).toLocaleString();const status=document.getElementById('status');status.textContent=state.summary.status;if(state.summary.status==='failed')status.classList.add('failed');renderSummary();buildSelections();restoreSelection();document.getElementById('filter').oninput=renderNavigation;renderNavigation();await renderDetail()}catch(error){document.getElementById('main').replaceChildren(element('div','error',text(error&&error.message||error)))}}start();
`;

// This viewer owns runner navigation only. Agent dump parsing and playback stay
// encapsulated in the archived report through its player-only URL contract.
export const testReportViewerHtml = String.raw`<!doctype html>
<html lang="en"><head><meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" /><title>Midscene Test Report</title><style>${viewerCss}</style></head>
<body><header class="topbar"><div><div class="brand"><span class="brand-mark" aria-hidden="true"></span><span>Midscene Test Report</span></div><div id="run" class="run">Loading run…</div></div><div id="status" class="status">Loading</div></header><div class="layout"><aside class="sidebar"><div id="summary" class="summary"></div><input id="filter" class="filter" type="search" placeholder="Filter projects, files, or cases" aria-label="Filter report results" /><div id="case-list"></div></aside><main id="main" class="main"><div class="loading">Loading test results…</div></main></div><script>${viewerScript}</script></body></html>`;

export const testReportViewerPath = (runDir: string): string =>
  join(runDir, 'report', 'index.html');

export const writeTestReportViewer = (runDir: string): string => {
  const path = testReportViewerPath(runDir);
  mkdirSync(join(runDir, 'report'), { recursive: true });
  writeFileSync(path, testReportViewerHtml, 'utf8');
  return path;
};
